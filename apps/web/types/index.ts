/**
 * DocuMind AI — Shared TypeScript Types (frontend)
 *
 * This file will be populated during feature implementation.
 * Types are derived from API_SPEC.md and DATABASE_SCHEMA.md.
 *
 * Do not add speculative types here — add them when the
 * corresponding feature is being implemented.
 */

// ---------------------------------------------------------------------------
// Placeholder — remove when first real types are added.
// ---------------------------------------------------------------------------
export type Placeholder = never;
